- 在当前同事版本基础上恢复本轮音频改动，尽量不覆盖现有结构：重新加入 BGM 预解锁阶段 -6dB 播放、首次输入后 2 秒恢复；恢复 BGM 启动重试字段与逻辑；将 KO 音效 U-05 / ui_ko_finish 提升到 +9dB；新增 BgmPause bus + AudioEffectLowPassFilter，实现暂停、KO、MATCH_OVER 的低通滤波，并在主菜单/模式选择/角色选择/地图选择/新回合开始时关闭滤波。
- 修复“目前完全听不到音乐”的新问题：BgmPause bus 可能继承了同事版本里已有的静音/音量效果，导致 BGM 全程被错误处理。现在在创建/刷新 BgmPause bus 时会清空其已有 effects，仅保留 low-pass；同时首次用户输入解锁音频时强制 stop+restart 当前 BGM，避免浏览器预启动造成 playing=true 但实际无声的假播放状态。
- 按用户截图中的旧方案回退 BGM 启动逻辑：未解锁前不再主动 play，只保留重试标记与预解锁低音量设定；首次输入后再真正 force restart 当前 BGM。这样避免浏览器/网页端进入“playing=true 但实际无声”的假播放状态。
- 继续排查“主界面和战斗都没有 BGM”：把 BgmPlayer 默认挂回 Master，只在暂停/KO/结算时临时切到 BgmPause bus，避免常驻自定义 bus 导致全程无声；同时扩展 BuildDebugLabel，实时显示当前 BGM stream、playing、volume、bus、unlocked、retry、key，便于下一轮直接判断是资源、总线还是解锁问题。
- 根据调试截图修复 BGM 启动卡死：截图显示 stream 已加载、bus=Master、unlocked=true，但 playing=false 且 retry=30，说明重试逻辑根本没被驱动。已把 _retry_bgm_start_if_needed() 接回 _physics_process() 每帧执行，并将所有 BGM play() 改为 play(0.0) 显式从头启动，避免某些 Godot/Web 情况下普通 play() 未真正进入播放状态。
- 修复主菜单/Neon Street BGM 资源仍不播放的问题：定位到自定义 _load_wav_stream_from_file() 仅支持 PCM WAV，项目里的 BGM_Main.wav 与 BGM_Neon Street.wav 虽然是 16-bit 48k 立体声，但通过手写解析路径加载时在运行中不稳定。现改为优先走 Godot 原生 ResourceLoader.load() 读取导入后的 AudioStream，仅在原生加载失败时才回退到手写 WAV 解析；保留循环设置，避免再次出现 bgm_main 与 bgm_neon_street 静音。
- 继续针对 Web 下 BGM play=false 排查：给 BgmPlayer 增加 PROCESS_MODE_ALWAYS，首次用户输入时直接 stop + play(0.0) 作为第一优先路径，不再完全依赖 _start_bgm_if_possible；并把调试信息扩展为 playing / has_stream_playback / is_inside_tree / stream_paused，方便判断是节点状态还是 Web 音频上下文问题。
- 根据用户截图继续定位 Web 下 BGM 点击后瞬时 playing=true 又回落 false：确认并修正重试终止条件，避免仅因 playing 短暂为 true 就把 bgm_retry_frames_left 清零。现在仅在 playing=true 且 has_stream_playback()=true 时才停止重试；首次解锁后的“已成功启动”判定也同步改为同时要求 has_stream_playback()，防止假启动导致后续彻底不再重试。
- 已按用户选择改为直接本地验证：确认 exports/web/index.html 存在，并已在内置预览中直接打开导出页，方便用户立刻点击测试主菜单与 Neon Street 场景下的 BGM 恢复情况。后台临时 http.server 因空闲自动结束，改为直接 file 预览作为本轮验证入口。
- 根据新截图继续缩小范围：菜单态仍出现 play/active 偶尔抖动但很快回落，说明 retry 仍可能被 `playing` 的瞬时值误杀。现进一步把“停止重试”和“解锁后判定成功”的条件都收紧为仅检查 `has_stream_playback()`，不再依赖 `playing`；同时在调试标签中新增 `pos`（播放位置）字段，方便下一轮判断是否真的开始推进时间轴。
- 从头排查 bgm_main / bgm_neon_street 不播放：确认 BGM_Main.wav 与 BGM_Neon Street.wav 资源存在且格式正常（16-bit PCM，48kHz，双声道），问题集中在运行时加载链路而非资源文件本身。修复方式为在 `fighting_game_test.gd` 中让 `_load_looping_bgm_stream()` 优先使用 Godot 导入后的原生 AudioStream（通过固定预载路径命中主菜单和 Neon Street 两首 BGM），仅在原生加载失败时才回退到手写 WAV 解析；同时更新 README，说明 BGM 默认优先读外部 WAV、失败才回退到生成音频。
- 对照用户提供的可正常播放版本 `C:/Users/xiaodi002/Downloads/scripts/fighting/fighting_game_test.gd` 复核后，确认当前工程与可播放版本在 BGM 核心差异并不在 `_input()` / `_unlock_audio_from_input()` / `_setup_audio()`，而是在 `_load_looping_bgm_stream()` 的加载顺序。已按可播放版本回退为“WAV 先走手写解析，失败后再走 ResourceLoader.load()”，并移除上一轮新增的 `BGM_PRELOAD_PATHS` 与 `_load_preimported_bgm_stream()`，使当前项目的 BGM 加载链路重新与用户验证可播的版本保持一致。
- 根据用户新截图继续修复：截图显示 `AudioStreamWAV | play=false | active=false | pos=0.00 | unlocked=true | retry=0 | key=bgm_main`，说明流已加载、音频已解锁，但主菜单切换到模式页后没有任何后续重试，BGM 处于“已拿到流但未真正启动”的静默状态。已在 `_play_bgm()` 中补一层解锁后的兜底启动：若当前已解锁且既不 `playing` 也没有 `has_stream_playback()`，则立即 `play(0.0)` 并恢复 `bgm_retry_frames_left`；同时在 `_unlock_audio_from_input()` 中把 `bgm_player.process_mode` 改回 `Node.PROCESS_MODE_INHERIT`，避免 `PROCESS_MODE_ALWAYS` 在菜单态引入与可播放版本不一致的节点处理行为。
- 在用户再次确认仍然无声后，已停止继续做局部补丁，改为把当前工程的 BGM 逻辑整体收敛到用户提供的“可播放版”实现：删除自定义的重试/预解锁音量恢复/低通总线暂停链路，`_setup_audio()`、`_play_bgm()`、`_start_bgm_if_possible()`、`_unlock_audio_from_input()` 以及菜单/暂停/回合结束相关调用均回退到下载版的简化实现，仅保留当前工程已有的调试状态显示（并去掉 `retry` 字段，因为该状态机已删除）。本轮目标是先让 `bgm_main` / `bgm_neon_street` 恢复到与对照脚本一致的最小可播路径。
- 用户授权按最有效方案推进后，已进一步加入“事件级 BGM 调试”而非继续凭状态猜测：新增 `bgm_debug_last_event` / `bgm_debug_event_count`，在 `_show_main_menu()`、`_show_mode_select()`、`_show_character_select()`、`_show_stage_select()`、`_play_bgm()`、`_start_bgm_if_possible()`、`_unlock_audio_from_input()` 打点，并把最近事件直接显示到左上角 BuildDebugLabel（格式 `evt#N event_name`）。这样下一轮可直接判断是根本没触发播放链、触发了但跳过、还是 play 后被后续流程打断。
- 用户回传截图时发现左上角只露出 `evt#` 但事件名被 UI 顶部裁掉，导致信息不够用。本轮已把单事件调试升级为短链路追踪：新增 `bgm_debug_event_log`，保存最近 6 个 BGM 事件，并在调试标签里追加 `trace=事件1 > 事件2 > ...`。下一轮截图即使尾部被截掉，通常也还能从 trace 看出完整调用顺序。
- 再次读取截图后确认当前信息仍被顶部 UI 截断，但已能判断这仍是老问题主线（BGM key 切换成功、audio 已 unlock、却持续 `play=false/active=false/pos=0.00`），不是资源/映射新问题。为拿到决定性证据，本轮继续增强可视化与运行探针：把 BuildDebugLabel 改成多行大面板，直接显示 `trace` 和新增的 `sig`（信号/探针）行；同时为 `bgm_player` 接入 `finished` 信号日志，并在每次 `play()` 后立即记录 `play_called ...`，再用 deferred probe 记录下一帧的 `playing/has_stream_playback/pos`。下一轮截图可直接分辨“play() 根本没生效”还是“生效后瞬间掉回 false”。
- 按用户要求继续做音效挂接：在 `scripts/fighting/fighting_game_test.gd` 的 `SOUND_PATHS` 中把默认战斗命中音效 `hit_light` / `hit_medium` 改为 `H-08_hit_punch_common_01~04`，把 `hit_heavy` 改为 `H-08_hit_kick_common_01~04`；同时补入当前仓库中已存在且与文档对应的 `H-07a/H-07b/H-07c` whoosh 映射、`miss_anchor/miss_coffin/miss_book` 别名，以及 `M-03a_jump_generic_01~06` 并让 `jump` 直接使用该组现有资源。仅挂接仓库里实际存在的资源，未为文档中缺失文件强行造占位。
- 按用户指定把角色移动脚步音效挂到 `assets/audio/SFX_M/0019 - VFAFS Boots Concrete - Audio - miss.wav`：在 `fighting_game_test.gd` 新增 `footstep` 映射并给出轻微音量偏移；在 `fighter_controller.gd` 新增 `footstep_cycle` 与 `_tick_walk_footstep()`，只在地面 WALK 状态下按步频触发 `sound_requested.emit("footstep")`，离开 WALK 状态时重置循环，避免站立或空中重复触发。
- 根据用户反馈修正脚步声“有点乱、静止时还在响”的问题：把脚步触发从输入分支内挪到 `move_and_slide()` 之后，只根据实际移动后的 `state/velocity/is_on_floor` 决定是否发声；`_tick_walk_footstep()` 现在在非 WALK、离地或水平速度很小时会立即清零计数并停止继续触发，只有真正持续移动时才按 18 帧步频重新发声。
- 针对“两个人一块走时脚步过于密集”，继续把脚步系统做得更合理：在 `fighter_controller.gd` 中把脚步间隔改成基于移动速度动态计算（慢走更稀、快走更紧），基础区间从约 30 帧到 20 帧；当检测到对手也在地面 WALK 时，再额外拉大 4 帧间隔降低总密度。同时加入按角色区分的相位错开（`p1` 先拍、`p2` 半拍偏移），避免两人同帧同时踩点造成“机关枪”式叠加。
- 根据用户反馈修正“有时候走路听不到脚步声”：保留双人错拍和降密度思路，但去掉进入 WALK 后必须等到全局相位命中的苛刻条件。现在角色一进入 WALK（或重新开始移动）就会立刻触发第一脚，并把 `footstep_cycle` 重新置零；`p2` 的错拍也从半拍缩到约 0.35 拍，减少长时间空等导致的漏响。后续持续移动仍按动态间隔和双人降密度规则运行，兼顾“别太密”和“别不响”。
- 用户继续反馈“还是偏密”后，再把脚步整体密度往下压一档：`_tick_walk_footstep()` 的动态间隔从原先约 `30~20` 调整到 `36~26` 帧；当检测到对手也在 WALK 时，额外间隔从 `+4` 提高到 `+8`。这样单人走路也会比之前更稀一点，双人同时走时则明显更松，不再那么连续轰炸。
- 用户进一步确认脚步资源其实是“约 10 秒的连续跑步脚步音效”，不是单步 one-shot。于是把实现方案从“定时重复触发短音效”改成“按角色启停循环长样本”：`fighting_game_test.gd` 中新增 `footstep_loop_players`，`p1/p2` 的 `sound_requested` 改为绑定来源 Fighter；`footstep` 事件首次触发时为该角色创建独立 `AudioStreamPlayer` 并循环播放该长样本，之后由 `_sync_footstep_loops()` 每帧根据角色是否处于地面 WALK 状态决定 play/stop，彻底避免长样本被反复叠播造成的机关枪感。对应地，`fighter_controller.gd` 里的 `_tick_walk_footstep()` 也简化为只负责发出一次 `footstep` 启动信号，而不再持续按节拍重复发射。
- 按用户要求重构音乐代码结构：从 `scripts/fighting/fighting_game_test.gd` 中移除原先 BGM 常量、BGM 路径表、生成音乐、手写 WAV 解析、BGM 调试状态等音乐相关逻辑，保留 SFX/SOUND_PATHS/脚步循环音效等音效系统；新增 `scripts/music_manager.gd` 作为短期可行的独立音乐管理器，统一管理菜单、选人、选地图、战斗音乐的加载、循环、音量和 Web 首次输入解锁；主脚本只保留轻量 wrapper 调用音乐管理器，并更新 README 的音乐说明。
- 用户确认脚步资源是长循环素材后，继续按这个方向重置脚步播放逻辑并修复“现在听起来没脚步声”：去掉上一版在 `fighter_controller.gd` 中残留的复杂节拍/错拍字段，只保留一次性发出 `footstep` 启动信号；在 `fighting_game_test.gd` 中把 `_play_footstep_loop()` 改成“没有播放器就先创建，有播放器就直接复用”，并在收到信号时立即按当前 WALK/落地/速度条件决定 `play(0.0)` / `stop()`，再由 `_sync_footstep_loops()` 持续维持状态。这样长脚步样本不再依赖一次性错过就永远不再触发的节拍器，走起来时能稳定起播，停下时也会停。
- 根据用户截图 debug 新音乐管理器：截图显示 `Music res://...BGM_Neon Street.wav` 已加载、`unlocked=true`，但 `play=false/pos=0.00`，说明资源和解锁都没问题，问题是首次 play() 后没有持续重试。已在 `scripts/music_manager.gd` 增加 `has_stream_playback()` 判定、`play(0.0)` 强制重启、120 帧重试窗口、每 10 帧 stop+play 重试，以及 `debug_last_event/debug_log/start_attempts/retry_frames_left`；左上角 BuildDebugLabel 现在显示 active/retry/attempts/evt/trace，便于下一张截图判断 play 调用是否生效或是否被打断。
- 按用户要求先把脚步音效相关代码整体删掉：从 `scripts/fighting/fighting_game_test.gd` 移除 `footstep` 的 `SOUND_PATHS`/音量偏移、`footstep_loop_players`、`_sync_footstep_loops()`、`_play_footstep_loop()` 以及 `_play_sfx()` 里的脚步特殊分支；从 `scripts/fighting/fighter_controller.gd` 移除 `footstep_cycle`、进入/退出 WALK 时的脚步状态复位、`_tick_walk_footstep()` 及其在移动流程里的调用。现在项目已回退到“没有脚步音效接入代码”的状态，便于后面重新设计。
- 根据用户最新音乐 debug 截图继续修复：截图显示 trace 已出现 `playback_active`，但当前 `play=false/active=false/pos=0.00`，说明音乐曾成功起播但随后停止，且 retry 已归零不再拉起。已在 `scripts/music_manager.gd` 增加 `desired_playing` 状态，只要当前期望播放但 `has_stream_playback()` 变 false，就会重新开启 120 帧重试并每 10 帧 `stop()+play(0.0)`；`unlock_audio_from_input()` 也只在 `desired_playing` 为 true 时强制起播；WAV 循环结束点改为按数据长度计算，避免 `loop_end=-1` 在部分环境下瞬停。BuildDebugLabel 增加 `desired` 字段用于判断是否应持续播放。
- 按用户新要求重新挂接脚步音效，但限制为“每个角色各自最多同时一个播放器、只有移动时播放、不移动时停止”：在 `scripts/fighting/fighting_game_test.gd` 恢复 `footstep` 的资源映射与音量偏移，新增 `footstep_players` 字典，并让 `_play_sfx()` 对 `footstep` 走单独分支；`_play_footstep_sfx()` 为每个角色懒创建一个专用 `AudioStreamPlayer`，把长脚步 WAV 设为循环，并只在当前处于地面 WALK 且速度足够时起播，否则停止；`_sync_footstep_players()` 每帧兜底同步，保证角色停下或离地时一定停播。`fighter_controller.gd` 侧新增 `footstep_playing` 状态与 `_sync_footstep_request()`，只在移动状态切换时发一次 `footstep` 信号，避免重复叠播。
- 修复“非战斗界面切换会让音乐重新播放”：`fighting_game_test.gd::_unload_battle_scene()` 现在只有确实存在战斗根节点或战斗 HUD 时才停止音乐，避免主菜单/模式菜单之间切换先 stop 再 play；`music_manager.gd` 增加 `current_path`，同一音频文件在菜单、选人、选图等不同 track key 间切换时不再重载/重播，保留进入战斗时可按战斗状态重新切换的行为。
- 根据用户截图继续修复首次点击“开始游戏”仍重播音乐的问题：`MusicManager.unlock_audio_from_input()` 现在只在当前没有有效 stream playback 时才启动，不再首次解锁时强制重启已经播放中的 BGM；同时将角色选择与地图选择的 `MUSIC_PATHS` 改为 `BGM_Main.wav`，让非战斗选择界面继续播放主菜单 BGM。
- 修复“音乐没播完就重新播”：`MusicManager` 新增 `_is_playback_active()`，把 `player.playing` 和 `has_stream_playback()` 都视为有效播放，避免 Web/Godot 下 `has_stream_playback()` 短暂为 false 时触发重试；重试逻辑不再先 `stop()` 再 `play(0.0)`，减少中途从头播放的风险，同时仍保留真正无播放时的补启动。
- 针对“战斗中角色倒下导致音乐重播”继续收敛 BGM 重试策略：确认倒地/回合结束流程本身没有直接调用播放战斗 BGM，重播主要来自 MusicManager 的自动补启动。新增 `playback_confirmed` 状态，BGM 一旦成功进入有效播放，当前 track 后续即使瞬时检测为非活跃也不再自动 `play(0.0)`；只有切换新 track 或 stop 后才重置确认状态并允许启动期补播，保证战斗音乐从进入战斗后一路播到战斗结束。
